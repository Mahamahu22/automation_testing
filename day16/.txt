import { test, expect } from '@playwright/test';

test('test', async ({ page }) => {
  await page.goto('http://dev.digitalbuildapp.com/#/menu.login');
  await page.getByRole('textbox', { name: 'Login Id' }).click();
  await page.getByRole('textbox', { name: 'Login Id' }).fill('mahalakshmi@digitalbuild.com.sg');
  await page.getByRole('textbox', { name: 'Password' }).click();
  await page.getByRole('textbox', { name: 'Password' }).press('CapsLock');
  await page.getByRole('textbox', { name: 'Password' }).fill('DB2024DEVUAT11');
  await page.getByRole('button', { name: 'Submit' }).click();
  await page.locator('#EPTW').click();
  await page.getByText('Summary').click();
  await page.getByRole('button', { name: '-Nov-2025' }).first().click();
  await page.getByRole('button', { name: 'Saturday, 1 November' }).click();
  await page.getByRole('button', { name: 'Done' }).click();
  await page.getByRole('button', { name: '21-Nov-' }).click();
  await page.getByRole('button', { name: 'Sunday, 30 November' }).click();
  await page.getByRole('button', { name: 'Done' }).click();
  const page1Promise = page.waitForEvent('popup');
  await page.getByTitle('Print').first().click();
  const page1 = await page1Promise;
  await page1.getByText('Block01/Level01/Unit01').click({
    button: 'right'
  });
  const page2Promise = page.waitForEvent('popup');
  await page.getByTitle('Print').nth(1).click();
  const page2 = await page2Promise;
  await page2.getByText('Project : Project 1 Category').click({
    button: 'right'
  });
});